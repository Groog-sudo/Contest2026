def generate_answer(question: str, contexts: list[dict[str, str | float]]) -> str:
    return (
        "This is a placeholder generated answer. "
        "Connect your LLM call here using the retrieved contexts."
    )

